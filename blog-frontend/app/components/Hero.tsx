/**
 * Hero.tsx — legacy standalone hero component.
 *
 * The hero is now managed via the Page Builder (heroSection block).
 * Use <BlockRenderer blocks={pageBuilder} /> in page.tsx instead.
 *
 * This file is kept for reference and is no longer rendered.
 */
export default function Hero() {
  return null;
}
